<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hloong.androidherotest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 自定义滚动view，仅供linearlayout使用 -->
        <activity android:name=".activity.ViewActivity" />
        <activity android:name=".activity.TouchActivity" />
        <activity
            android:name=".activity.BitmapActivity"
            android:theme="@style/AppThemeFive" />
        <activity android:name=".activity.RecyclerActivity" />
        <activity android:name=".activity.SystemInfoActivity" />
        <activity android:name=".activity.ListViewActivity" />
        <activity android:name=".activity.TouchFlowActivity" />
        <activity android:name=".activity.SlideMenuActivity" />
        <activity android:name=".activity.ClockActivity" />
        <activity android:name=".activity.BitmapCanvasActivity" />
        <activity android:name=".activity.MatrixActivity" />
        <activity android:name=".activity.CircleImageActivity" />
        <activity android:name=".activity.GuaGuaKaActivity"></activity>
    </application>

</manifest>